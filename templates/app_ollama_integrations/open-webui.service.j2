[Unit]
Description = Open-WebUI
After = network-online.target

[Service]
Type = simple
User = ubuntu
Group = ubuntu
WorkingDirectory = {{ my_path }}/open-webui
ExecStart = {{ my_path }}/open-webui/open-webui.sh
Restart = on-failure
TimeoutSec = 15

[Install]
WantedBy = multi-user.target
